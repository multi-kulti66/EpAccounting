<UserControl x:Class="EpAccounting.UI.View.UserControl.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:userControl="clr-namespace:EpAccounting.UI.View.UserControl"
             xmlns:viewModel="clr-namespace:EpAccounting.UI.ViewModel"
             d:DataContext="{d:DesignInstance viewModel:ClientViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="../ViewResourceDictionary.xaml" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <userControl:ClientEditView Grid.Column="0" DataContext="{Binding Path=ClientEditViewModel}" />
        <GridSplitter Grid.Column="1" Style="{StaticResource VerticalGridSplitterStyle}" />
        <userControl:ClientSearchView Grid.Column="2" DataContext="{Binding Path=ClientSearchViewModel}" />

    </Grid>
</UserControl>
